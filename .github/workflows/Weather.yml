name: Weather Fetch Test

on:
  workflow_dispatch:

jobs:
  fetch-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch weather for Brisbane
        run: |
          echo "📍 Brisbane weather data:"
          curl -s "https://api.open-meteo.com/v1/forecast?latitude=-27.47&longitude=153.03&current=temperature_2m&timezone=auto"
